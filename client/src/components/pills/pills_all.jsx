import styles from './pills.module.scss'

const Pills_all = () => {
  return (
    <div className={styles.pill}>
      <button className={styles.one}>All</button>
    </div>
  )
}

export default Pills_all
